import { colors } from '../../styles/theme';

/*eslint-disable*/
const styles = {
  base: {
    color: colors.secondary,
    lineHeight: '5rem',
    textAlign: 'center',
    width: '100%',
  },

  primary: { color: colors.tertiary },

  warning: { color: colors.accent },
};
/*eslint-enable*/

export default styles;
